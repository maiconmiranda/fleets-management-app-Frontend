import "./Main.css";
import DashHome from "./DashHome";

// Here is where the home dashboard is first called to display
const Main = () => {
  return (
    <dash_main>
      {/* all the data for main dashboard */}
      <DashHome />
    </dash_main>
  );
};

export default Main;
